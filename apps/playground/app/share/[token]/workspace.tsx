"use client";

import { useState } from "react";

type SharedItem = { id: string; brand: string; brief: string; status: "pending" | "approved" | "edited" | "flagged"; renderId: string; assets: Array<{ format: string; filename: string }>; note?: string };
type Comment = { id: string; itemId?: string; author: string; text: string; createdAt: string };
export type SharedBatch = { id: string; title: string; createdAt: string; items: SharedItem[]; comments?: Comment[] };

export function ApprovalWorkspace({ token, initialBatch }: { token: string; initialBatch: SharedBatch }) {
  const [batch,setBatch]=useState(initialBatch); const [cursor,setCursor]=useState(0); const [author,setAuthor]=useState(""); const [comment,setComment]=useState(""); const [busy,setBusy]=useState(false); const [message,setMessage]=useState<string|null>(null); const item=batch.items[cursor];
  async function review(action:"approve"|"flag"){if(!item)return;setBusy(true);const res=await fetch(`/api/share/${encodeURIComponent(token)}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({itemId:item.id,action,note:comment||undefined})});const next=await res.json() as SharedItem&{error?:string};if(res.ok){setBatch((current)=>({...current,items:current.items.map((entry)=>entry.id===next.id?next:entry)}));setMessage(action==="approve"?"Approved. The team can now schedule it.":"Changes requested. Your note is attached.");}else setMessage(next.error??"Action failed");setBusy(false);}
  async function addComment(){if(!author.trim()||!comment.trim()||!item)return;setBusy(true);const res=await fetch(`/api/share/${encodeURIComponent(token)}`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({author,text:comment,itemId:item.id})});const body=await res.json() as {comments?:Comment[];error?:string};if(res.ok){setBatch((current)=>({...current,comments:body.comments??current.comments}));setComment("");}else setMessage(body.error??"Could not add comment");setBusy(false);}
  if(!item)return <main className="mx-auto max-w-3xl px-6 py-20"><h1 className="font-display text-4xl font-bold">Nothing to review</h1></main>;
  const assets=item.assets.filter((asset)=>asset.format==="og-image"||asset.format==="ig-carousel"); const comments=(batch.comments??[]).filter((entry)=>entry.itemId===item.id);
  return <main className="min-h-screen bg-[#F4F0E8] text-ink"><header className="border-b border-[#C9C4BA] bg-ink px-5 py-4 text-bone"><div className="mx-auto flex max-w-6xl items-center justify-between"><div className="font-display font-bold">brandrail <span className="font-mono text-[10px] font-normal text-muted">CLIENT REVIEW</span></div><div className="font-mono text-[10px] text-muted">SECURE LINK · NO ACCOUNT REQUIRED</div></div></header><div className="mx-auto grid max-w-6xl gap-6 px-5 py-8 lg:grid-cols-[1fr_340px]"><section><p className="font-mono text-[10px] uppercase tracking-[.16em] text-[#A83200]">{batch.title}</p><h1 className="font-display text-4xl font-bold mt-3">{item.brief}</h1><div className="mt-3 flex items-center gap-3"><span className="border border-[#C9C4BA] px-2 py-1 font-mono text-[10px]">{item.brand}</span><span className="font-mono text-[10px] text-[#6A655D]">{cursor+1} / {batch.items.length}</span><span className={`font-mono text-[10px] uppercase ${item.status==="approved"?"text-green":"text-[#A83200]"}`}>{item.status}</span></div><div className="mt-6 grid gap-3 sm:grid-cols-2">{assets.map((asset)=><img key={asset.filename} src={`/api/share/${encodeURIComponent(token)}/asset/${encodeURIComponent(item.renderId)}/${encodeURIComponent(asset.filename)}`} alt={`${item.brand} ${asset.format}`} className="w-full border border-[#C9C4BA] bg-white object-contain"/>)}</div><div className="mt-5 flex justify-between"><button className="border border-ink px-4 py-2 text-sm disabled:opacity-30" disabled={cursor===0} onClick={()=>setCursor((value)=>value-1)}>← Previous</button><button className="border border-ink px-4 py-2 text-sm disabled:opacity-30" disabled={cursor===batch.items.length-1} onClick={()=>setCursor((value)=>value+1)}>Next →</button></div></section><aside className="h-fit border border-[#C9C4BA] bg-white p-5 lg:sticky lg:top-5"><p className="font-mono text-[10px] uppercase tracking-[.15em] text-[#A83200]">Decision</p><h2 className="font-display text-2xl font-bold mt-2">Ready to ship?</h2><p className="text-sm text-[#6A655D] mt-2">Approving does not publish automatically. The team still controls timing and channels.</p><input className="mt-5 w-full border border-[#C9C4BA] px-3 py-2 text-sm" placeholder="Your name" value={author} onChange={(e)=>setAuthor(e.target.value)}/><textarea className="mt-2 min-h-24 w-full border border-[#C9C4BA] px-3 py-2 text-sm" placeholder="Comment or requested change…" value={comment} onChange={(e)=>setComment(e.target.value)}/><div className="mt-3 grid grid-cols-2 gap-2"><button disabled={busy} onClick={()=>void review("flag")} className="border border-ink px-3 py-3 text-sm">Request changes</button><button disabled={busy} onClick={()=>void review("approve")} className="bg-[#A83200] px-3 py-3 text-sm font-semibold text-white">Approve</button></div><button disabled={busy||!author.trim()||!comment.trim()} onClick={()=>void addComment()} className="mt-2 w-full border border-[#C9C4BA] px-3 py-2 text-xs disabled:opacity-40">Add comment only</button>{message&&<p className="mt-3 text-xs text-[#A83200]">{message}</p>}<div className="mt-6 border-t border-[#E2DED6] pt-4"><p className="font-mono text-[10px] uppercase text-[#6A655D]">Comments ({comments.length})</p><div className="mt-3 space-y-3">{comments.map((entry)=><div key={entry.id}><div className="text-sm font-semibold">{entry.author}</div><p className="text-sm text-[#514D47]">{entry.text}</p></div>)}</div></div></aside></div></main>;
}
